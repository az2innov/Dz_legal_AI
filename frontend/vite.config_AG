import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: "dist",        // Antigravity sert ce dossier
    emptyOutDir: true
  },
  server: {
    port: 5173,            // pour ton dev local
  },
  preview: {
    port: 4173             // pour tester le build local
  }
})